# Kafka server properties


broker.id={{ item.id }}
listeners=PLAINTEXT://{{ hostvars[item.hostname].ansible_host }}:9092
log.dirs={{ kafka_data_dir }}
num.partitions=1
zookeeper.connect={{ zookeeper_connect }}
